services:
  # Backend Service
  - type: web
    name: scam-deducer-backend
    env: node
    region: singapore
    plan: free
    buildCommand: cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: PORT
        value: 10000
    autoDeploy: false

  # Frontend Service
  - type: static
    name: scam-deducer-client
    repo: https://github.com/nithurani10-maker/qr # Optional explicitly
    buildCommand: cd client && npm install && npm run build
    staticPublishPath: client/dist
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: VITE_API_URL
        fromService:
          type: web
          name: scam-deducer-backend
          property: url
